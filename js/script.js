function replace1(text, photo) {
    document.getElementsByClassName('hello-text')[0].innerText = text;
    document.getElementsByClassName('photo')[0].src = photo;
}

function replace2(text, photo) {
    document.getElementsByClassName('hello-text')[1].innerText = text;
    document.getElementsByClassName('photo')[0].src = photo;
}

function replace3(text, photo) {
    document.getElementsByClassName('hello-text')[2].innerText = text;
    document.getElementsByClassName('photo')[0].src = photo;
}